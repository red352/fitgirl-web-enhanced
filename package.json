{
  "name": "fitgirl-web-enhanced",
  "version": "1.5.0",
  "description": "优化 FitGirl Repacks 的信息密度、宽屏布局、折叠内容与热门榜单。",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "red352",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:fixture": "vite --config test/e2e/vite.config.ts",
    "test:e2e": "npm run build && playwright test",
    "test:live": "npm run build && node test/live-qa.mjs",
    "capture": "npm run build && node scripts/capture-assets.mjs",
    "check": "npm run lint && npm run format:check && npm run test && npm run build",
    "bump": "node scripts/bump-version.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.10.13",
    "eslint": "^10.9.1",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.0",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.57.1",
    "vite": "^8.2.2",
    "vite-plugin-monkey": "^8.1.1",
    "vitest": "^4.1.11"
  }
}
